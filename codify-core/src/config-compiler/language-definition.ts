export enum ConfigClass {
  PLUGIN = 'plugin',
  PROJECT = 'project',
  RESOURCE = 'resource',
}

export enum ResourceParameterType {
  ARRAY = 'array',
  BOOLEAN = 'boolean',
  NULL = 'null',
  NUMBER = 'number',
  OBJECT = 'object',
  STRING = 'string',
}
